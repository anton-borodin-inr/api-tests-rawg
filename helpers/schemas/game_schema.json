{
  "type": "object",
  "required": ["id", "name", "released", "rating"],
  "properties": {
    "id": {"type": "integer"},
    "name": {"type": "string", "minLength": 1},
    "released": {"type": ["string", "null"]},
    "rating": {"type": "number", "minimum": 0, "maximum": 5}
  }
}